# Coffee OS 云服务器部署指南

> 适用环境：腾讯云 Ubuntu 22.04 LTS，4核4G  
> 部署方式：Docker（MySQL 8 + 后端 API + 前端 Nginx 三个容器）  
> 更新时间：2026-08-14

---

## 0. 部署架构

```
用户浏览器 / 手机
      │
      ▼
   Nginx 容器（80 端口）
      │
      ├── /          → H5 静态页面
      ├── /api/      → 反代到后端容器 :3000
      └── /uploads/  → 反代到后端容器（桌码图片等）
              │
              ▼
        后端 API 容器（Node.js + Prisma）
              │
              ▼
        MySQL 8 容器（数据持久化）
```

---

## 1. 服务器准备

### 1.1 腾讯云控制台

- 系统：Ubuntu 22.04 LTS（已重装完成 ✓）
- 安全组放行端口：
  - `22`（SSH）
  - `80`（HTTP，部署后访问）
  - `443`（HTTPS，配置证书后）
- 记录服务器公网 IP

### 1.2 连接服务器

```bash
# 本机终端执行（密码登录）
ssh root@<服务器公网IP>
```

---

## 2. 获取代码

两种方式任选：

### 方式 A：git clone（推荐，方便后续更新）

先把项目推到 GitHub / Gitee / 腾讯工蜂，然后在服务器上：

```bash
apt update && apt install -y git
git clone git@github.com:zitangkou/coffee-order.git
cd coffee-order
```

### 方式 B：本机打包上传

本机（Mac）执行：

```bash
cd ~/Projects
tar czf coffee-order.tar.gz --exclude=node_modules --exclude=dist --exclude=.git coffee-order
scp coffee-order.tar.gz root@<服务器公网IP>:~/
```

服务器上解压：

```bash
apt update && apt install -y tar
tar xzf coffee-order.tar.gz
cd coffee-order
```

---

## 3. 配置部署参数

```bash
cd coffee-order
cp deploy/.env.example .env
nano .env   # 用 vim 也行
```

必改项：

| 变量 | 说明 | 示例 |
| --- | --- | --- |
| `HTTP_PORT` | 对外访问端口 | `80` |
| `WEB_BASE_URL` | 对外访问地址（生成桌码用） | `http://47.100.xx.xx` 或 `https://coffee.example.com` |
| `MYSQL_PASSWORD` | MySQL 密码（改成自己的） | 一串强密码 |
| `MYSQL_ROOT_PASSWORD` | MySQL root 密码（改成自己的） | 一串强密码 |
| `JWT_SECRET` | 登录令牌密钥（改成自己的） | 随机长字符串 |

保持默认即可的项：

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `VITE_API_BASE` | `/api` | 前端同源调 API，一般不用改 |
| `MOCK_PAYMENT` | `true` | H5 模拟支付；接微信支付后改 `false` |
| `PRINTER_ENABLED` | `false` | 云打印机接入后改 `true` |

---

## 4. 一键部署

```bash
./deploy.sh
```

脚本会自动完成：

1. 安装 Docker 与 Docker Compose（如果没装）
2. 配置腾讯云镜像加速（`mirror.ccs.tencentyun.com`）
3. 构建并启动 MySQL + 后端 + 前端三个容器
4. 自动建表、写入种子数据
5. 健康检查 + API 验证

看到以下输出即成功：

```
{"code":0,"data":{"ok":true},"msg":"success"}
部署完成！
```

> 首次构建需要拉取基础镜像，约 3–8 分钟，取决于网络。

---

## 5. 手动安装 Docker（备选）

如果不想用脚本自动装，也可以手动装完再执行 `./deploy.sh`：

```bash
# 1. 官方脚本安装 Docker
curl -fsSL https://get.docker.com | sh
systemctl enable --now docker

# 2. 安装 Docker Compose v2
apt-get install -y docker-compose-plugin
# 或手动安装：
# mkdir -p /usr/local/lib/docker/cli-plugins
# curl -SL https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64 \
#   -o /usr/local/lib/docker/cli-plugins/docker-compose
# chmod +x /usr/local/lib/docker/cli-plugins/docker-compose

# 3. 配置腾讯云镜像加速
mkdir -p /etc/docker
cat > /etc/docker/daemon.json <<'EOF'
{
  "registry-mirrors": ["https://mirror.ccs.tencentyun.com"]
}
EOF
systemctl restart docker

# 4. 验证
docker version
docker compose version
```

然后回到部署流程：`cp deploy/.env.example .env` → `./deploy.sh`。

---

## 6. 验证部署

```bash
# 容器状态（三个都应是 Up / healthy）
docker compose ps

# API 健康检查
curl http://localhost/api/health

# 看后端日志
docker compose logs -f server
```

浏览器访问：

- 顾客端首页：`http://<服务器IP>/#/pages/index/index`
- 商家后台：`http://<服务器IP>/#/pages_admin/login/index`
- 商家账号：`admin / admin123`（**上线前务必修改**）

---

## 7. 生成桌码

1. 登录商家后台 → 桌台管理
2. 确认 `.env` 里 `WEB_BASE_URL` 已是公网地址
3. 每个桌台点「生成桌码」→ 下载二维码图片 → 打印贴桌
4. 吧台可再做一个「外带码」（进入后手动切外带即可）

> 注意：改过 `WEB_BASE_URL` 后需重新生成桌码。

---

## 8. 域名 + HTTPS（备案完成后）

### 8.1 域名解析

在腾讯云 DNSPod 把域名 A 记录指向服务器公网 IP。

### 8.2 配置 HTTPS

```bash
# 安装 certbot
apt install -y certbot python3-certbot-nginx

# 申请证书并自动配置 Nginx（nginx 在容器里，需先改端口方案，见下方说明）
certbot --nginx -d coffee.example.com
```

> 本项目 Nginx 运行在容器内。配置 HTTPS 有两种方式：
>
> 1. 简单方式：在宿主机再装一个 Nginx 做 443 终结，反代到容器的 80 端口；
> 2. 或把证书文件挂载进 web 容器并改 `deploy/nginx.docker.conf` 增加 443 server 块（需要时我可以帮你改）。

### 8.3 更新 `.env`

把 `WEB_BASE_URL` 改成 `https://coffee.example.com`，重新生成桌码。

---

## 9. 安全加固（上线前必做）

```bash
# 1. 修改商家后台默认密码（登录后台操作）或改种子数据后重建

# 2. 修改 SSH 端口并禁止 root 密码登录（建议改用密钥）
#    腾讯云控制台生成密钥对，或：
ssh-keygen   # 本机生成公钥
ssh-copy-id root@<服务器IP>
sed -i 's/^#Port 22/Port 2222/' /etc/ssh/sshd_config
sed -i 's/^#PermitRootLogin yes/PermitRootLogin prohibit-password/' /etc/ssh/sshd_config
systemctl restart sshd

# 3. 开启 UFW 防火墙（只放行必要端口）
ufw allow 22
ufw allow 80
ufw allow 443
ufw enable

# 4. 定期备份 MySQL 数据卷
#    手动备份：
#    docker run --rm -v coffee-order_mysql_data:/data -v ~/backup:/backup alpine tar czf /backup/mysql-$(date +%F).tar.gz -C /data .
```

建议把「数据备份」加入 crontab（每天凌晨备份一次）：

```bash
crontab -e
# 添加一行：
0 3 * * * docker run --rm -v coffee-order_mysql_data:/data -v ~/backup:/backup alpine tar czf /backup/mysql-$(date +\%F).tar.gz -C /data . 2>/dev/null || true
```

---

## 10. 日常运维

| 操作 | 命令 |
| --- | --- |
| 查看容器状态 | `docker compose ps` |
| 看后端日志 | `docker compose logs -f server` |
| 看前端/MySQL 日志 | `docker compose logs -f web` / `docker compose logs -f mysql` |
| 重启全部 | `docker compose restart` |
| 重启单个 | `docker compose restart server` |
| 更新代码并发布 | `git pull && docker compose up -d --build` |
| 停止全部 | `docker compose down` |
| 彻底清理（含数据卷，慎用） | `docker compose down -v` |
| 进 MySQL 命令行 | `docker compose exec mysql mysql -ucoffee -p coffee_os` |

---

## 11. 常见问题

### 拉取镜像很慢 / 超时

```bash
# 确认镜像加速已配置
cat /etc/docker/daemon.json
# 换源后重启 docker 再试
systemctl restart docker
docker compose up -d --build
```

### 80 端口被占用

```bash
ss -lntp | grep :80
# 如果被其他程序占用，改用其他端口：.env 里 HTTP_PORT=8080 后重新部署
```

### 后端健康检查失败 / API 报 502

```bash
docker compose logs server   # 看后端报错
docker compose logs mysql    # 看数据库是否正常
docker compose ps            # 确认 mysql 是否 healthy
```

常见原因：MySQL 首次初始化慢，等 1–2 分钟重启 server：`docker compose restart server`。

### 桌码扫出来打不开

确认 `.env` 的 `WEB_BASE_URL` 是公网可访问的地址，且安全组放行了对应端口；改完后重新生成桌码。

### 想重置数据（谨慎）

```bash
docker compose down -v        # 删除数据卷
docker compose up -d --build  # 重新建表 + 种子数据
```

---

*部署完成后，建议把本文档第 9 节的安全项全部执行一遍再正式营业。*
