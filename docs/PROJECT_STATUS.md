# Coffee Order · 项目进度与上下文记忆

> 本文件用于记录项目进度、关键决策、环境上下文与后续待办，作为后续开发会话的交接记忆。
> 更新时间：2026-08-26

---

## 1. 项目概况

- 项目名称：**coffee-order**（本地路径 `/Users/dnn/Projects/coffee-order`）
- 产品名：Coffee OS · 单店精品咖啡店扫码点单系统
- 目标形态：微信小程序优先发布；顾客端 H5 暂缓，商家内部 Web 后台保留
- 仓库：`git@github.com:zitangkou/coffee-order.git`（main 分支，已推送，工作区干净）
- 需求来源：5 份 AI 需求文档合并整理，见 [docs/咖啡点单系统-综合需求与设计文档.md](咖啡点单系统-综合需求与设计文档.md)

## 2. 技术栈与架构

| 层 | 技术 |
| --- | --- |
| 前端 | uni-app（Vue3 + TypeScript + Pinia），一套代码编译 H5 + 微信小程序 |
| 后端 | Node.js + TypeScript + Express + Prisma |
| 数据库 | 本地开发 SQLite；生产 MySQL 8（`server/prisma/schema.mysql.prisma`） |
| 部署 | Docker Compose（MySQL + 后端 + 前端 Nginx 三容器），Ubuntu 22.04 |

架构：`浏览器 → Nginx(:80) → /api 反代 → Node(:3000) → MySQL`，Nginx 同时托管 H5 静态产物与 `/uploads`（桌码图）。

## 3. 开发进度（按阶段）

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| Phase 0 | 项目初始化、Prisma 数据模型、seed、Design Tokens | ✅ 完成 |
| Phase 1 | 后端 API：鉴权、分类/商品/桌台、订单/支付、退款、统计、设置 | ✅ 完成，冒烟测试通过 |
| Phase 2 | 顾客端 H5：扫码进店、菜单、规格定制、购物车、下单、取餐码、退款 | ✅ 完成，双端编译通过 |
| Phase 3 | 商家后台：看板、订单工作台、商品/桌台管理、统计、设置 | ✅ 完成，双端编译通过 |
| Phase 4 | 联调打磨：手机实测、UI 走查、异常态 | 🟡 进行中（功能收尾已补齐并回归，缺真实设备走查） |
| Phase 5 | 部署准备：Docker 一键部署、PM2/Nginx 配置、部署文档 | ✅ 完成 |
| Phase 6 | 微信小程序适配：登录、微信支付、订阅消息 | ✅ 代码骨架完成（登录/支付/订阅），待资质接入、真机调试与提审 |

## 4. 已完成功能清单

**顾客端**

- 扫码进入（URL 带 `table_id` 自动绑定桌号，可切外带）
- 首页：店铺信息、分类横滑、商品网格、底部购物车条
- 商品详情：规格组选择（杯型/温度/奶/浓度/加料，实时计价；加料组支持多选）、备注、加购
- 购物车：增删改数量、整单备注、合计
- 确认订单：堂食（选桌）/外带（手机号后四位）、包装费、提交 + 模拟支付
- 支付结果：大号取餐码、查看订单/返回首页
- 我的订单：状态筛选、订单详情（状态进度条、退款申请、10s 轮询 + 出餐弹窗提醒）

**商家端**

- 登录鉴权（JWT，默认账号 admin/admin123）
- 经营看板：今日营收/订单数/客单价/待接单
- 订单工作台：状态 Tab、接单/出餐/完成、10 秒轮询、退款审核（同意/拒绝）
- 商品管理：增删改、上下架、售罄开关、规格 JSON 编辑
- 桌台管理：增删、启停、生成/查看桌码二维码、外带码
- 数据统计：日/周/月营收、热销排行、时段分布
- 系统设置：店名、营业开关、堂食/外带、包装费、退款开关、测试打印、修改密码
- 商品图片：商家端上传（multer，5MB 限制，存 uploads/products/）
- 后端定时任务：自动取消超时未支付订单（默认 15 分钟，`ORDER_TIMEOUT_MINUTES` 可调）

## 5. 关键决策与偏差记录

1. **前端 uni-app 一套代码双端**（来源文档多数派，避免二次重写）。
2. **本地开发 SQLite、生产 MySQL**：因本机网络拉取 MySQL 镜像失败，本地改用 SQLite（`schema.prisma`），生产用 `schema.mysql.prisma`；Docker 镜像构建时会自动切换 MySQL schema。
3. **首发仅接微信支付**；模拟支付只允许本地/测试环境，生产环境 fail-closed。
4. **规格模型**：`specsJson` 存为 JSON 字符串（SQLite 不支持 Json 列），读写均做序列化/反序列化（`server/src/lib/json.ts`），MySQL 下同样兼容。
5. **退款 P0**：顾客申请 + 商家审核（文档决策）。
6. **会员/储值放 P1**，首期不做。
7. 订单状态机：`UNPAID → PAID → MAKING → READY → COMPLETED`，另有 `REFUNDING/REFUNDED/CANCELLED`。

## 6. 环境与常用命令（上下文记忆）

### 本机（Mac）

- 项目路径：`/Users/dnn/Projects/coffee-order`（注意：之前是 coffee_order，已重命名）
- 后端启动：`cd server && npm run dev`（监听 3000，SQLite 在 `server/prisma/dev.db`）
- 前端启动：`cd web && npm run dev:h5`（监听 5173）
- 数据初始化：`cd server && npx prisma db push && npm run seed`（幂等）
- 双端构建：`cd web && npm run build:h5` / `npm run build:mp-weixin`
- 本机当前状态（2026-08-14 检查）：后端 :3000 运行中；H5 dev :5173 已停止，需要时重启
- 注意：沙箱曾限制端口监听/网络，本机运行服务需在沙箱外执行

### 默认账号与密钥

- 商家后台：`admin / admin123`（上线前必须修改）
- 本地 JWT_SECRET：`coffee-os-dev-secret-2026`（server/.env，已被 gitignore）
- 生产 .env：由 `deploy/.env.example` 复制，需改 `WEB_BASE_URL`、MySQL 密码、`JWT_SECRET`

### 云服务器（腾讯云）

- 系统：Ubuntu 22.04 LTS，4核4G，公网 IP `124.223.178.64`
- 部署方式：Docker 一键（`./deploy.sh`），详见 [docs/部署指南.md](部署指南.md)
- 服务器拉代码走 SSH 失败（私有仓库无 Deploy Key），已给方案：生成 `~/.ssh/coffee_deploy` 公钥加到 GitHub Deploy Keys（只读）
- 登录服务器后流程：`git clone` → `cp deploy/.env.example .env` → `nano .env` → `./deploy.sh`

## 7. 后续待开发清单（TODO）

### 上线前（P0 收尾）

- [x] **商品图片上传**（2026-08-14：后端 multer 接口 + 商家端传图按钮）
- [x] **订单取餐通知**（2026-08-14：订单详情 10s 轮询，READY 弹窗）
- [x] **未支付订单自动取消**（2026-08-14：定时任务）
- [x] **多选加料**（2026-08-14：规格组 `multiple: true`）
- [x] **外带码**（2026-08-14：桌台管理生成/查看）
- [x] **商家端修改密码**（2026-08-14：设置页）
- [x] **后端冒烟测试**（2026-08-16：`npm run smoke`，22 项用例）
- [ ] **真实设备走查**：手机浏览器实测 H5 全流程，收集 UI/交互反馈
- [ ] **真实商品图**：目前为占位图（首字+底色），需提供实际图片上传
- [x] **服务器部署**：已按方案 B 部署至腾讯云（网关 + 127.0.0.1:8080），待域名备案后切正式域名
- [ ] **安全加固落地**：强制改密已生效；`JWT_SECRET`/MySQL 密码改强、UFW 防火墙、SSH 密钥登录待执行

### 微信小程序（Phase 6）

- [x] 代码骨架：微信登录（openid）、JSAPI 支付参数、订阅消息（2026-08-16，未配置资质 fail-closed）
- [ ] 接入 AppID/Secret、商户号 + APIv3 密钥 + 商户证书、订阅模板 ID
- [ ] 真机调试 → 合法域名配置 → 隐私协议 → 提审发布

### 打印对接

- [ ] 对接飞鹅/易联云云打印机：填写 `printerConfig`，`PRINTER_ENABLED=true`，实现 `server/src/services/printer.ts` 中的 HTTP 推送（当前为 stub）
- [x] 商家端测试打印 UI 已就绪（/admin/printer/test）

### P1（二期）

- [x] 轻会员（咖啡档案）：手机号绑定 + 验证码、消费记录、等级徽章（2026-08-16）
- [ ] 储值与余额支付：`member_accounts`/`wallet_logs`/`recharge_records` 表已建（**明确暂缓**，待稳定熟客后评估）
- [x] 数据导出：统计页 CSV 导出（Excel 可打开）
- [ ] 基础满减优惠券
- [ ] 语音提醒增强（已有 Web Audio 新单提示音）
- [ ] 经营周报：规则化每周经营摘要（营收对比/热销变化/退款提醒），AI 分析的低成本替代

### 安全（P0 已完成，其余分级）

- [x] 接口/登录限流、支付回调验签与幂等（fail-closed）、强制改密、密码强度、JWT 收紧、Nginx 安全头、数据库备份脚本
- [ ] 全站 HTTPS：待域名 + ICP 备案
- [ ] 依赖漏洞扫描、监控告警（服务宕机/支付失败激增）

### P2（远期）

- [ ] AI 经营分析、销售预测、客户画像、菜单优化建议（**暂缓**，数据积累 3–6 个月后评估）
- [ ] 个性化推荐

### 工程化

- [x] 后端自动化测试：`npm run smoke`（22 项接口回归）
- [ ] 前端 E2E 测试
- [ ] HTTPS/域名（备案完成后）+ 证书自动化
- [x] MySQL 自动备份脚本（`deploy/backup.sh` + `deploy/install-backup.sh`，服务器上需执行安装）
- [ ] 商品图片云存储迁移（当前存服务器本地 uploads/）

## 8. 待收集资料

- 店铺名称 / Logo / 主色最终确认
- 首期商品清单与商品图
- 云打印机型号
- 微信小程序 AppID + 商户号
- 域名与备案进度
- 服务器 SSH 登录方式确认（Deploy Key 或密钥）
- 短信服务商（会员手机号绑定正式启用，阿里云/腾讯云）

---

*更新进度时同步维护本文件，保证后续会话可以快速接续。*

## 11. 部署与协作进展（2026-08-15 晚）

- 云服务器（腾讯云 124.223.178.64，Ubuntu 22.04）已按**方案 B（域名网关）**切换：coffee compose 显式 `name: coffee-order`，web 只监听 `127.0.0.1:8080`；宿主机 Nginx 做网关（`server_name _` 转发 8080）。
- Docker 全局配置已拆为一次性 `deploy/setup-docker.sh`；`deploy.sh` 仅构建启动本项目。
- 多项目：`docs/多项目部署指南.md` + 可复制模板 `deploy/project-template/`（8081 起步）。
- 兄弟项目 zhixing-gongkao（知行公考）确认已自带 Docker 化（8081，H5+Python 后端+管理后台），无需套模板；过渡期直接 IP:8081，正式期走网关域名 server 块。注意其 Dockerfile 禁用 alpine（Taro binding 需 glibc）。
- 待办：迭代 C（安全 P0：支付回调验签/HTTPS/接口限流/数据库备份自动化；会员储值；监控告警）。支付与 HTTPS 相关项在正式营业前必须完成。

---

## 9. 迭代 A（P0）完成记录 · 2026-08-15

按 [docs/优化方案.md](优化方案.md) 完成点单体验闭环：

- 规格系统去 JSON 化：新增 `SpecGroup / SpecOption / ProductSpecGroup` 三张表，商品移除 `specsJson`，附一次性迁移脚本（`npm run dump:specs` → `prisma db push --accept-data-loss` → `npm run migrate:specs`）。
- 商品新增 `isSignature / isHot` 标记：顾客端提供「全部 / 招牌 / 热销」快捷筛选 + 分类抽屉（底部浮层，带搜索）。
- 顾客端：菜单页「＋」改为规格弹层加购（`components/SpecSheet.vue`）；规格默认值自动选中（`isDefault`，单选必选组默认第一个）；堂食/外带与桌号持久化，切回堂食无桌号自动弹选桌。
- 商家端：商品页新增搜索/分类/状态筛选；商品编辑改为可视化规格配置（勾选规格组 + 必选开关 + 新建规格组表单）；登录页居中；看板指标可跳转订单/统计。
- 冒烟测试扩展至 15 项（含规格组 CRUD、商品搜索、商品规格关联），全部通过；H5 与微信小程序双端构建通过。

## 10. 迭代 B（P1）完成记录 · 2026-08-15

- 管理员体系：`Admin.status`（启用/禁用）、管理员增删改接口（仅 MANAGER）、角色权限隔离（店员可接单/售罄，店长可管商品/设置/退款/管理员）。
- 审计日志：`AuditLog` 表 + 订单状态、退款审核、商品、设置、管理员、规格组等关键操作留痕，商家端「管理员」页可查最近 50 条。
- 并发安全：订单状态流转与退款审核改为原子条件更新（`updateMany where 旧状态`），双人同时操作只有一个成功。
- 订单步骤条：`components/StepBar.vue` 四步流程（已支付→制作中→待取餐→已完成），退款/取消用独立状态卡。
- 统计扩展：近 7 天营收趋势、品类销售占比、退款统计，数据可导出 CSV（Excel 可打开）。
- 冒烟测试扩展至 18 项（原子流转、管理员权限隔离、审计日志、统计扩展），全部通过；双端构建通过。

## 11. 迭代 C 安全 P0 完成记录 · 2026-08-15

- 接口限流：`express-rate-limit`，全局默认 300 次/分钟/IP；登录接口 5 次/15 分钟/IP（防暴力破解），阈值与开关可配（`RATE_LIMIT_DISABLED / API_RATE_LIMIT / LOGIN_RATE_LIMIT`）。
- 支付回调：`services/payment.ts` 实现微信支付 v3 回调验签（RSA-SHA256）+ AES-256-GCM 解密 + 金额二次校验 + 交易号幂等；**fail-closed**——未配置商户证书/密钥时回调一律拒绝；冒烟测试已覆盖。
- 密码策略：管理员密码最少 8 位；新建管理员强制首登改密；默认密码 `admin123` 被检测到时强制开启改密（`Admin.mustChangePassword`）；修改密码后清除标记。
- 登录态：管理员 JWT 有效期由 7 天收紧为 2 天。
- Nginx：安全响应头（X-Content-Type-Options / X-Frame-Options / Referrer-Policy / CSP）。
- 数据库备份：`deploy/backup.sh`（mysqldump + gzip + 保留 14 天）+ `deploy/install-backup.sh`（crontab 每日 03:00）。
- 冒烟测试扩展至 19 项，全部通过；双端构建通过。

**仍依赖外部条件的项（代码/配置已就绪，待激活）**：

- 全站 HTTPS：待域名 + ICP 备案（证书与 Nginx 网关配置见部署文档）。
- 微信支付正式接入：待商户号 + APIv3 密钥 + 平台证书（配置 `WECHAT_API_V3_KEY` / `WECHAT_PLATFORM_CERT_PATH` 后自动启用验签回调）。
- MySQL 端口：容器默认不暴露公网（compose 仅内网 expose），已满足。

## 12. 轻会员（咖啡档案）完成记录 · 2026-08-16

- 手机号绑定：`POST /auth/send-code`（验证码 5 分钟有效、60 秒重发冷却）+ `POST /user/phone`（绑定校验、防重复绑定），短信默认 console 模式（验证码打印日志/接口返回），生产可切换短信服务商。
- 消费档案：`GET /user/profile` 返回累计消费、消费次数、最近消费时间、等级与最近 20 条消费记录（按已支付订单统计，退款/取消不计入）。
- 等级体系：累计消费自动升级（咖啡新友 → 咖啡常客 ¥500 → 咖啡老友 ¥1500 → 咖啡大师 ¥3000），含升级进度。
- 顾客端：新增「咖啡档案」页（首页入口），未绑定手机号时引导绑定；已绑定展示等级徽章、统计卡片、进度条与消费记录。
- 冒烟测试扩展至 20 项（发码/绑定/档案），全部通过；双端构建通过。

**明确暂缓**：储值（含 MemberAccount/WalletLog/RechargeRecord 表已建，待有稳定熟客与资金合规准备后再做）；AI 分析（待数据积累 3–6 个月后评估）。

## 13. 微信小程序 Phase 6 代码骨架完成记录 · 2026-08-16

- 微信登录：`POST /auth/wx-login`（code 换 openid，`User.wxOpenid` 唯一绑定）；前端小程序端优先微信登录，失败回退游客登录。
- 微信支付：`POST /orders/:id/pay` 按 `X-Platform: mp-weixin` 区分——小程序端返回 `wx.requestPayment` 参数（JSAPI 下单 + RSA 签名，未配置资质 fail-closed），H5 端保持模拟支付；下单页接入收银台拉起与取消处理。
- 订阅消息：`POST /user/subscribe` 保存授权；订单出餐（READY）时向已订阅微信用户推送取餐通知（best-effort）；支付成功页引导订阅（模板 ID 未配置时跳过）。
- 平台适配：`utils/platform.ts` 条件编译封装 `wx.login / requestPayment / requestSubscribeMessage`，H5 端零影响。
- 配置项：`WECHAT_MP_APPID / WECHAT_MP_SECRET / WECHAT_MCH_ID / WECHAT_MCH_SERIAL / WECHAT_MCH_PRIVATE_KEY_PATH / WECHAT_SUBSCRIBE_TEMPLATE_READY`（compose 与 .env.example 已就绪）。
- 冒烟测试扩展至 22 项（mp 支付 fail-closed、H5 回退、微信登录未配置报错、订阅保存），全部通过；双端构建通过。

**待资质就绪后**：填 AppID/Secret/商户号/证书/模板 ID → 微信开发者工具导入 `web/dist/build/mp-weixin` 真机调试 → 配置合法域名 → 提审发布。

## 14. 正式营业前检查清单

### 资质与环境

- [ ] 域名完成 ICP 备案，全站 HTTPS 生效（含网关 server 块与证书自动化）
- [ ] 微信小程序资质：AppID/Secret、商户号、APIv3 密钥、商户证书、订阅模板 ID
- [ ] 微信支付回调验签通过真机验证，`MOCK_PAYMENT=false`
- [ ] 短信服务商接入，会员手机号绑定走真实验证码
- [ ] 云打印机型号确认并完成对接（如需要自动打印）

### 服务器安全

- [ ] 默认管理员密码已改（强制改密流程完成）
- [ ] `.env` 中 `JWT_SECRET`、MySQL 密码为强密码
- [ ] UFW 只放行 22/80/443；SSH 密钥登录、禁止 root 密码登录
- [ ] `bash deploy/install-backup.sh` 已安装每日备份，并做一次恢复演练

### 内容与配置

- [ ] 店铺名称 / Logo / 主色最终确认并配置
- [ ] 真实商品图全部上传，菜单价格/规格与实物核对
- [ ] 营业时间、公告、堂食/外带、包装费、退款开关核对
- [ ] 桌码、外带码重新生成并打印贴桌

### 流程走查

- [ ] 手机真机走查：扫码 → 点单 → 支付 → 出餐 → 退款全流程
- [ ] 商家端双人同时操作（接单/出餐）验证原子状态流转
- [ ] 异常场景：售罄商品不可下单、停业开关生效、支付失败/取消处理

## 15. 首页分类改版（吸顶分类条 + 锚点联动）· 2026-08-17

- 问题：真实分类藏在「当前分类 ▾」抽屉里，按分类找商品需 2 次点击；「全部」平铺无分组；分类条不吸顶，滚动中无法切换。
- 方案（对比过美团/瑞幸式左右分栏，为保留大图双列视觉选择此方案）：
  - 吸顶分类条直接露出全部胶囊：全部 / 招牌 n / 热销 n / 各分类 + 商品数（空分类不显示），选中胶囊随 `scroll-into-view` 自动滚入可视区；
  - 商品区按分区渲染（招牌推荐 → 热销榜单 → 各分类，带分区标题）；点胶囊锚点滚动，滚动列表 scrollspy 反向高亮（60ms 节流 + 点击后 600ms 锁定防闪烁）；
  - 售罄商品置底 + 灰显（opacity 0.55）；移除冗余分类抽屉。
- 改动仅 `web/src/pages/index/index.vue`；H5 与微信小程序双端构建、首页类型检查通过（products/platform/request 的剩余类型错误为既有问题）。
- 同轮减法优化（竞品调研结论，吸取 coco/瑞幸的减法型优点）：hero 压缩为两行紧凑头部（首屏露出分类条+首排商品）；整卡点击直弹规格层、商品名点击才进详情页（点单路径去掉一次跳页）。
- 待验证：浏览器/真机走查吸顶与锚点联动实际手感。

## 16. 微信小程序正式发布审查 · 2026-08-22

**结论**：当前达到“可编译、可演示、可继续真机联调”，尚不满足正式营业发布条件。

### 验证结果

- 后端 `npm run build`：通过。
- 微信小程序 `npm run build:mp-weixin`：通过，产物约 456 KB。
- 前端 `npm run type-check`：未通过，共 5 处既有错误（角色/规格联合类型、缺少 `adminDeleteProduct`、支付 provider 类型、PATCH 请求类型）。

### 上线阻断项（P0）

- [x] 生产禁用模拟支付：服务层强制检查 `MOCK_PAYMENT`，生产启动时若为 `true` 直接拒绝启动；Compose/部署示例默认 `false`。
- [x] 移除小程序支付后的模拟支付兜底；前端改为支付后轮询服务端订单状态，超时提示稍后从订单列表确认。
- [x] 订单详情、支付、退款全部强制用户鉴权并校验 `order.userId`，禁止匿名或跨用户访问、支付和退款。
- [ ] 接入微信真实退款 API、退款回调、幂等、失败重试和对账；审核通过不能直接等同资金退款成功。
- [x] 已预留 `secrets/` 并只读挂载到 `/run/secrets`；真实证书到位后放入目录并配置路径，证书轮换仍需联调验证。
- [ ] 隐私合规大部分完成：已有政策/协议、手机号用途说明、双确认账号注销、身份数据匿名化及旧令牌失效；仍待经营主体/客服/实际留存期限确认和小程序后台隐私保护指引。

### 提审前工程与配置项（P1）

- [ ] 填真实 AppID，正式构建开启 URL 校验，配置备案 HTTPS 的 request/uploadFile 合法域名。
- [ ] 订阅模板 ID 改为构建配置注入，保持前后端一致。
- [ ] 生产缺失强 `JWT_SECRET` 时拒绝启动；限制 CORS；短信从 console 切换真实服务商。
- [x] 修复全部前端类型错误；新增 `scripts/release-check.sh`，覆盖 type-check、双端构建、后端构建和生产模拟支付安全门禁。
- [x] 商家后台保留 H5，并通过 `pages.json` 条件编译从顾客小程序产物排除。
- [ ] 真机验证扫码、弱网、重复点击、取消支付、回调延迟/重复、退款、订阅通知、停业和售罄场景。

审查知识已同步到公共 Obsidian：`00-公共知识库/项目/Coffee OS.md`。

## 17. 无资质依赖优化第一轮完成 · 2026-08-22

- 订单安全：新增强制用户鉴权中间件；订单详情、支付、退款、会员档案和订阅保存不再接受匿名请求；订单操作统一校验用户归属。
- 模拟支付：服务层 fail-closed；生产环境启用模拟支付或使用弱 JWT 密钥时服务拒绝启动；Docker 生产默认关闭模拟支付。
- 支付体验：小程序收银台成功后最多轮询约 12 秒等待服务端 `PAID`，不再把支付结果未知的订单转为模拟支付。
- 浏览器安全：生产 CORS 限制为 `CORS_ORIGINS/WEB_BASE_URL` 配置来源，小程序和服务间无 Origin 请求保持兼容。
- 前端质量：修复原有 5 处 TypeScript 错误，并同步修正管理员密码长度前端校验。
- 端边界：微信小程序构建仅包含顾客端、会员和协议页面；商家后台继续只在 H5 构建中提供。
- 合规骨架：新增隐私政策和用户服务协议草稿页，明确标注提审前必须补充的经营主体、客服及留存信息。
- 自动化：冒烟测试扩展至 23 项，新增匿名/跨用户订单攻击回归；`23/23` 通过。一键发布检查覆盖前端类型、H5/小程序构建、后端构建及生产模拟支付门禁，全部通过。

**当时仍等待外部资料**：真实 AppID/Secret、微信支付商户资料与证书、退款/查单联调、订阅模板、备案 HTTPS 合法域名、小程序后台隐私指引。账号注销技术流程已在第二轮按“身份匿名化、必要交易留存”原则实现，正式文本仍需经营主体确认。

## 18. 无资质依赖优化第二轮完成 · 2026-08-23

- 账号生命周期：`User` 增加状态、令牌版本和注销时间；注销采用双确认，清除微信标识、手机号、头像和订阅授权，保留必要历史交易；存在进行中订单/退款或余额时阻止注销；令牌版本递增使所有旧 JWT 立即失效。
- 下单完整性：服务端严格校验营业开关、堂食/外带开关、堂食桌号、非空订单、商品数量、必选/单选/多选规格、无效及重复规格，避免绕过前端创建非法订单。
- 支付幂等：微信交易号增加数据库唯一约束；回调改为交互式事务和条件状态更新，并处理并发重复回调；超时取消后收到真实成功支付可恢复为已支付，避免资金已到账但订单丢失。
- 主动查单：新增微信商户订单查询能力和 `/orders/:id/payment-status`，小程序支付确认轮询会在资质配置后自动调用微信主动查单；未配置时 fail-closed 且不改变订单状态。
- 证书部署：Compose 将仓库本地 `secrets/` 只读挂载至容器 `/run/secrets`，目录内容默认忽略，仅保留 `.gitkeep`；私钥和平台证书不会进入 Git 或镜像。
- 防刷：短信、下单和支付增加独立 IP 限流，阈值通过 `SMS_RATE_LIMIT / ORDER_RATE_LIMIT / PAYMENT_RATE_LIMIT` 配置。
- 发布门禁：增加生产 MySQL schema 校验，并自动检查微信小程序产物不得包含商家后台。
- 验证：SQLite schema 已升级，变更前确认现有支付交易号无重复；MySQL schema 校验通过；前端类型检查、H5/小程序构建、后端构建通过；冒烟测试扩展至 25 项并 `25/25` 通过。

**仍依赖外部资料**：真实微信登录、统一下单/主动查单真机联调、微信退款 API 与退款回调、商户证书轮换验证、订阅模板、备案 HTTPS 合法域名、最终经营主体/客服信息和后台隐私保护指引。

## 19. 首发范围与后续规划（简洁版）· 2026-08-26

**范围决定**：首发只做微信小程序顾客端；顾客端 H5、H5 支付和公开 H5 适配暂缓。商家内部 Web 后台继续保留，用于接单、商品、桌台、退款和经营管理。

1. **现在完成**：生产迁移流程、服务器安全与备份、日志/监控、真实图片域名、桌码小程序码、订阅消息状态闭环、真机异常态检查。
2. **认证完成后**：接入 AppID/Secret，验证微信登录、合法域名、隐私指引、扫码场景值和体验版真机流程。
3. **支付开通后**：完成统一下单、支付回调、主动查单、关单、退款、退款回调、幂等和对账。
4. **上线前**：全流程 UAT、弱网/重复操作回归、备份恢复演练、生产配置检查、小程序提审与灰度发布。
5. **首发后评估**：根据实际需求再决定是否恢复顾客端 H5、优惠券、储值和云打印。

## 20. 小程序部署前期工程第一批 · 2026-08-26

- 生产数据库由危险的启动时 `db push --accept-data-loss` 改为提交 migration 后执行 `prisma migrate deploy`；提供新库初始化与旧库基线切换开关，seed 只允许显式执行。
- 4C4G 容器资源基线：MySQL 1.5G、API 768M、Web 128M；日志限制为 10MB × 3，并限制 MySQL buffer pool 和连接数。
- 增加两层反向代理识别配置，限流可获取真实客户端 IP；Web 容器补健康检查。
- 正式小程序微信登录失败不再降级游客；小程序包排除游客登录和全部商家后台 API。
- 新增 `VITE_MP_API_BASE` / `VITE_ASSET_BASE`，小程序 API 和商品图片可使用合法 HTTPS 绝对域名；正式发布门禁拒绝空值和示例域名。
- 新增无限小程序码后端骨架和商家后台入口，支持 `table_<id>` 桌码与 `takeout` 外带场景；首页可解析 `scene`。
- 订阅消息发送采用 `ACCEPTED → SENDING → SENT` 状态流转，并发只消费一次，失败恢复待发送状态。
- 验证通过：前端类型检查、小程序/H5 构建、后端构建、安全门禁、MySQL schema、Compose 配置和 25 项冒烟测试。现存仅 Sass 旧 API/`@import` 弃用警告。

**下一批**：备份恢复脚本与演练说明、服务器安全检查清单自动化、真实域名构建配置；AppID 到位后联调登录和小程序码。

## 21. 小程序部署前期工程第二批 · 2026-08-29

- 明确安全边界：发布资质、支付信息及证书目录不参与本地搜索、构建扫描和会话输出；代码检查只判断布尔就绪状态，不显示配置值或文件路径。
- 公开构建配置已固定为 `https://nagacoffee.site/api` 和 `https://nagacoffee.site`，正式小程序产物会拒绝 localhost、后台 API、游客登录及敏感配置标记。
- 宿主机 Nginx 模板更新为 `nagacoffee.site` 的 80 → 443 跳转、TLS 1.2/1.3、回环端口反代和基础安全响应头。
- 备份脚本不再 `source .env`，改用容器内部认证；备份采用私有权限、gzip 完整性检查、SHA-256 校验和 14 天轮转。
- 新增非破坏性恢复演练：将备份导入随机临时库、检查业务表数量后自动删除，永不覆盖生产库；定时任务为每日备份、每周恢复验证。
- 新增服务器只读安全检查：验证 Compose、容器、端口暴露、回环绑定、UFW 和公网 HTTPS，不打印环境变量。
- 新增微信发布就绪检查：仅报告登录、支付下单、支付回调和订阅消息的 `ready/not-ready`，不输出任何配置值。
- 正式发布门禁再次通过：脚本语法、类型检查、小程序/H5 构建、后端构建、安全门禁及 MySQL schema 均通过；仍有 Sass 弃用警告。

**当前外部阻断**：`nagacoffee.site` DNS/HTTP 已存在，但 2026-08-29 外部检查 TCP 443 仍连接超时。需在服务器完成证书、宿主机 Nginx、腾讯云安全组和 UFW 443 配置后，再运行 `deploy/security-check.sh`。备份恢复脚本已通过语法检查，真实恢复演练需在服务器 MySQL 运行后执行。

## 22. 小程序支付退款闭环与提审门禁 · 2026-08-29

- 微信资金链路补齐：统一下单带超时时间；未支付订单先向微信关单再本地取消；已支付响应会主动查单并阻止误取消。
- 原路退款不再把“管理员同意”视为资金成功：退款状态拆分为 `PENDING / PROCESSING / SUCCESS / FAILED / REJECTED`，支持退款申请、回调、主动查询和每 5 分钟处理中补偿同步。
- 退款结果校验商户退款单号、订单号及金额；同一退款单号重复提交保持幂等。成功为不可逆终态，迟到的异常回调不会回退已退款订单；失败可被后续成功结果纠正。
- 退款申请使用原子条件更新，避免同一订单并发生成多条退款；后台增加明确二次确认、处理中/失败展示和手动同步入口。
- 生产 migration 增加退款资金字段与唯一退款单号；经营统计只把真实退款成功计入退款额，同时兼容历史已同意记录。
- Docker 上下文新增环境文件、私密目录及常见私钥/证书格式排除规则；小程序产物门禁扫描本地地址、后台 API、游客登录、私钥标记及敏感配置名。
- 订阅模板改为私密构建环境注入；最终提审门禁同时要求有效 AppID，不打印注入值。
- 新增 `docs/小程序发布执行清单.md`，按服务器、微信后台、支付联调、真机验收、提审和灰度发布组织操作顺序。
- 验证通过：发布脚本语法、前端类型检查、小程序/H5 构建、后端编译与安全门禁、退款状态流转/终态单调/幂等/并发测试、生产 MySQL schema。此前 HTTP 冒烟测试为 `26/26`；本轮新增并发与乱序场景由独立数据库状态机测试覆盖。

**仍需外部条件**：公网 443/TLS/Nginx 实装、有效 AppID 与微信登录、合法域名和隐私指引、微信支付商户开通及真实支付/退款/回调/对账、订阅模板、经营主体与客服文本、体验版真机 UAT。私密发布资料继续只在受控目录或部署环境中使用，不进入 Git、文档及会话。

## 23. 发布前全面复盘 · 2026-08-29

完整证据、优先级和验收标准见 `docs/发布前全面复盘-2026-08-29.md`。

**新增 P0 结论**：

- 下单缺少端到端幂等，未支付订单缺少继续支付/主动取消入口。
- 取餐码只有 900 个候选且查询全部历史订单，无数据库唯一约束，必须改为按营业日生成。
- 管理员禁用、降权、重置密码和首次强制改密尚未在服务端实时约束旧令牌。
- 生产短信仍可能使用 console 并返回测试验证码；游客 API 在生产仍开放。
- 小程序 `urlCheck` 仍关闭，法律文本仍有草稿提示，订阅授权在页面加载时自动触发。
- 支付回调需补商户号/AppID/币种校验和单订单成功支付约束；真实支付后尚未触发打印。
- 数据库备份未覆盖上传图片 volume，容器业务时区未固定，今日/小时统计可能偏移。
- 生产镜像仍包含开发工具并以 root 运行，Compose 的数据库弱默认密码需改为启动前 fail-closed。

**依赖审计**：后端生产依赖 0 个已知漏洞；前端依赖树 32 个（11 高、10 中、11 低），主要集中在 uni-app 构建链及间接依赖。需整套升级并回归，不直接执行破坏性的强制修复。

**范围决定不变**：微信小程序顾客端首发，商家 Web 后台保留；顾客 H5、储值、AI 和营销增长功能继续暂缓，先完成正确性、安全、合规和可恢复性。

---

*当前主线为微信小程序首发；资质未到位时优先完成不依赖微信审核与商户资料的工程项。*
